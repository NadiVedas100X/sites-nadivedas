'use client';
import {useState,type FormEvent} from 'react';
export default function AffiliateForm(){const[busy,setBusy]=useState(false),[error,setError]=useState(''),[success,setSuccess]=useState(false);
async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const form=new FormData(e.currentTarget);try{const response=await fetch('https://love.nadivedas.com/api/affiliates',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'apply',name:form.get('name'),email:form.get('email'),phone:form.get('phone'),profession:form.get('profession'),audience:form.get('audience'),profileUrl:form.get('profileUrl'),promotionPlan:form.get('promotionPlan'),password:form.get('password'),website:form.get('website'),consent:form.get('consent')==='on'})});const data=await response.json();if(!response.ok)throw new Error(data.error||'Please try again.');setSuccess(true)}catch(err){setError(err instanceof Error?err.message:'Please try again.')}finally{setBusy(false)}}
if(success)return <div className="af-form af-success" role="status"><span>✓</span><h3>Your application is with us.</h3><p>Thank you for introducing yourself. Our team will review your profile. Your account remains pending until approval.</p><p>Keep your chosen password safe. You can sign in after your application is approved.</p><a className="af-button" href="https://love.nadivedas.com/partner-login">Go to partner login →</a></div>;
return <form className="af-form" onSubmit={submit}><h3>Apply to join the Partner Circle</h3><p>Tell us a little about yourself.</p><div className="af-form-row"><label>Full name<input name="name" autoComplete="name" maxLength={120} required/></label><label>Email address<input name="email" type="email" autoComplete="email" maxLength={240} required/></label></div><div className="af-form-row"><label>Phone / WhatsApp<input name="phone" type="tel" autoComplete="tel" placeholder="Include country code" minLength={8} maxLength={40} required/></label><label>Your work<select name="profession" required defaultValue=""><option value="" disabled>Select your profession</option><option>Astrologer</option><option>Tarot reader</option><option>Spiritual practitioner / healer</option><option>Creator / educator</option><option>Community organiser</option><option>Other</option></select></label></div><label>Website or social profile <small>(optional)</small><input name="profileUrl" type="url" placeholder="https://" maxLength={400}/></label><label>Tell us about your community<textarea name="audience" placeholder="Who do you reach? Include languages, locations and approximate audience size." maxLength={1000} required/></label><label>How would you introduce NadiVedas?<textarea name="promotionPlan" placeholder="For example: recommendations to interested clients or educational posts for your community." maxLength={2000} required/></label><label>Create your partner password<input name="password" type="password" autoComplete="new-password" minLength={12} maxLength={128} required/><small>At least 12 characters. Login activates after approval.</small></label><label className="af-honey" aria-hidden="true">Website<input name="website" tabIndex={-1} autoComplete="off"/></label><label className="af-consent"><input name="consent" type="checkbox" required/><span>I agree to be contacted about my application and to promote responsibly, disclose affiliate relationships and avoid spam or misleading claims. I have read the <a href="/privacy">Privacy Policy</a>.</span></label>{error&&<p className="af-error" role="alert">{error}</p>}<button className="af-button" disabled={busy}>{busy?'Sending your application…':'Send my application'} <span>↗</span></button><small className="af-form-foot">Your details go securely to the NadiVedas team for review.</small></form>}
