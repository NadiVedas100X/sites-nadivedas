import type {Metadata} from "next";import Link from "next/link";import {SiteHeader,SiteFooter} from "../components/SiteChrome";import {journalPosts} from "../lib/journal";
export const metadata:Metadata={title:"Nadi Astrology Guides & Palm Leaf Reading Journal",description:"Clear guides to Nadi astrology, thumbprint classification, the 13 Kandams, sacred lineages and preparing for a palm-leaf reading.",alternates:{canonical:"/blog"}};
export default function Page(){const [featured,...posts]=journalPosts;return <main className="new-site journal-index"><SiteHeader/><header className="journal-hero"><div><span className="kicker">The NadiVedas journal</span><h1>Ancient wisdom,<br/><em>clearly understood.</em></h1><p>Substantive, honest guides to leaf finding, sacred lineages and preparing for a Nadi reading—without mystery used as pressure.</p></div><div className="journal-seal"><b>6</b><span>Complete<br/>field guides</span></div></header><section className="journal-body"><Link href={`/blog/${featured.slug}`} className="journal-feature"><div><span>{featured.category} · {featured.readTime}</span><h2>{featured.title}</h2><p>{featured.description}</p><strong>Read the complete guide →</strong></div><aside><small>START HERE</small><b>01</b><p>Understand the tradition before choosing a reading.</p></aside></Link><div className="journal-grid">{posts.map((post,index)=><article className="journal-card" key={post.slug}><header><span>{String(index+2).padStart(2,"0")}</span><small>{post.category}</small></header><h2>{post.title}</h2><p>{post.description}</p><footer><small>{post.readTime}</small><Link href={`/blog/${post.slug}`}>Read guide <span>→</span></Link></footer></article>)}</div></section><section className="journal-cta"><div><span className="kicker">Ready when you are</span><h2>Your search begins with one clear step.</h2><p>Book leaf finding for ₹999. If no matching leaf is found, the fee is refunded.</p></div><Link href="/journey" className="lux-btn">Begin leaf finding <span>→</span></Link></section><SiteFooter/></main>}
